---
title: "Network Daily Digest - 2026-06-26"
date: 2026-06-26
permalink: /network/2026-06-26
---

# Network Daily Digest - 2026-06-26

> 6 月 26 日的資料中心網路主軸落在「**AWS EFA 推進到 700 Gbps + NVIDIA RTX PRO 4500 Blackwell Server Edition 量產**」與「**中國 LineShine LX2 互連細節補完**」與「**arXiv 6/25 罕見無新 DC fabric 論文**」三條軸線：一、AWS 於 6 月 18 日 GA **[Amazon EC2 G7 instances](https://aws.amazon.com/blogs/aws/announcing-amazon-ec2-g7-instances-accelerated-by-nvidia-rtx-pro-4500-blackwell-server-edition-gpus/)**，**首次由主要 hyperscaler 搭載 NVIDIA RTX PRO 4500 Blackwell Server Edition GPU + 700 Gbps EFA-enabled networking（7× G6）+ 7.6 TB local NVMe + GPUDirect RDMA over EFA + AWS Exemplar Cloud status for GB300 training**——這是 **2026 年中「hyperscaler 對 AI fabric 的『throughput-per-node』目標從 400 Gbps 推到 700 Gbps」最量產化的硬體里程碑**。AWS 是 **第一個主要雲** 支援 NVIDIA RTX PRO 4500 Blackwell Server Edition、g7.48xlarge / g7.metal 提供 8× GPUs、256 GB GPU memory、**700 Gbps of EFA-enabled networking throughput**、支援 **NVIDIA GPUDirect P2P + GPUDirect RDMA with EFA + GPUDirect RDMA with EFA for Amazon FSx for Lustre**——多 GPU + 多 node 的 low-latency GPU-to-GPU 互連架構正式在 AWS 量產。NVIDIA 同步在 6/24 公開 [NVIDIA and AWS Collaborate to Bring AI to Production at Scale](https://blogs.nvidia.com/blog/nvidia-aws-ai-production-scale/) 總結這次合作是 **「EC2 G7 RTX PRO 4500 Blackwell + cuVS 進入 OpenSearch Serverless 預設 + AWS 取得 NVIDIA Exemplar Cloud status for GB300 training」三件套**。二、The Next Platform 6/25 補完 [A Deep Dive On China's "LineShine" All-CPU, Exaflops-Class Supercomputer](https://www.nextplatform.com/hpc/2026/06/25/a-deep-dive-on-chinas-lineshine-all-cpu-exaflops-class-supercomputer/5262439)，披露 **LX2 chiplet 架構 1.55 GHz / 690W / 304 active cores per socket（48 blocks × 4 cores × 2 chiplets, 384 raw cores, 79.2% yield）/ 16 HBM stacks (8 per chiplet, 64 GB total HBM, 8 TB/sec bandwidth) / 256 GB LPDDR5X DRAM / LingQi 4-layer fat-tree 1.6 Tb/sec per node (2× 400 Gb/sec ports) / 3.5 Pb/sec bi-section bandwidth / 1.07μs single-hop latency / 80.35% computational efficiency / 22,680 nodes / 42.2 MW total**——這是 **2026 年中「中國自研 HPC CPU + 自研互連在 exascale 達到 NVIDIA Quantum-X800 InfiniBand 等級 latency」的硬體層面首次完整揭露**。Prickett Morgan 公開判斷 **「LingQi is reasonably sure is based on a variation of InfiniBand technology but it could be a jacked-up and stripped-down version of Ethernet」**，但 **1.07μs single-hop latency + Layer 1 銅纜 / Layer 4 光纖** 的設計更像 **「InfiniBand-style switched fabric 在 Ethernet physical layer 上」** 的 hybrid——這對 **UEC（Ultra Ethernet Consortium）的 transport 設計哲學** 與 **NVIDIA Quantum-X800 的 latency target** 都是新基準。arXiv 6/25 出現 **「cs.NI 罕見無新 DC fabric 論文」** 的 24-hour 空白：cs.NI/recent 抓到的 75 篇中 **沒有任何一篇符合 RDMA / InfiniBand / DPU / SmartNIC / datacenter network / congestion control / kernel bypass / programmable switch / NVLink / NVSwitch / CXL strict_terms** 過濾後仍 in-window 的 paper；6/24 仍以 NVIDIA Vera Rubin 7 Exaflops ISC 2026 報導、arXiv 2606.20581 RDMACell 為主要近期 DC fabric 研究內容。開源社群方面，**PMIx v5.0.11（6/23 發布）修補 OmniPath support component (pnet/opa) 與 OMPI default MCA params**——這是 **2026 年中「HPC process management 在 Intel OmniPath fabric 上重新恢復支援」** 對 Intel 已停產 OmniPath hardware 的 legacy 用戶（Argonne Aurora、CSCS Piz Daint 等）是 release 級重要性。**NFS-Ganesha V10.0（6/19 發布）** 是 NFS-Ganesha 十年來的 major version 升級（NFSv4.2 + RDMA + GPUDirect 支援強化），對 HPC parallel file system 在 AI training 場景的 reference deployment 是新基準。

---

## 今日頭條

**AWS EC2 G7 正式 GA：NVIDIA RTX PRO 4500 Blackwell Server Edition + 700 Gbps EFA-enabled networking + GPUDirect RDMA over EFA + 7.6 TB local NVMe — 第一個主要雲** —— 6 月 18 日，AWS 在 [Announcing Amazon EC2 G7 instances accelerated by NVIDIA RTX PRO 4500 Blackwell Server Edition GPUs](https://aws.amazon.com/blogs/aws/announcing-amazon-ec2-g7-instances-accelerated-by-nvidia-rtx-pro-4500-blackwell-server-edition-gpus/) 公開 **EC2 G7 instances 全面 GA**。**核心定位**：「**AWS is the first major cloud provider to support NVIDIA RTX PRO 4500 Blackwell Server Edition GPUs**」——這是 **2026 年中 hyperscaler 對「**AI fabric per-node throughput 推上 700 Gbps**」這個新 baseline 的第一個量產化硬體**。**核心規格**：(i) **GPU**——**NVIDIA RTX PRO 4500 Blackwell Server Edition**，**32 GB GPU memory per GPU**、**5th Gen Tensor Cores + 4th Gen RT Cores**、**1.33× GPU memory capacity vs G6 + 2.45× GPU memory bandwidth vs G6**、**最多 8 GPUs per instance (256 GB total GPU memory)**；(ii) **CPU**——**custom 6th Gen Intel Xeon Scalable processors**、**最多 192 vCPUs + 768 GiB system memory**；(iii) **Networking**——**700 Gbps of EFA-enabled networking throughput（7× G6）**、**NVIDIA GPUDirect P2P for multi-GPU sizes**、**NVIDIA GPUDirect RDMA with EFA**、**GPUDirect RDMA with EFA for Amazon FSx for Lustre**——這是 **「**EC2 內部直接支援 RDMA over EFA 給 multi-GPU + multi-node 場景**」** 的 production deployment；(iv) **Storage**——**最多 7.6 TB local NVMe SSD**（1.5× G6）、**EBS bandwidth 80 Gbps on g7.48xlarge**；(v) **Video**——**9th Gen NVENC + 6th Gen NVDEC + 4:2:2 編解碼**、**1.5× concurrent video streams vs G6**。**Instance size matrix**：g7.2xlarge (1 GPU, 8 vCPU) / g7.4xlarge (1, 16) / g7.8xlarge (1, 32) / g7.12xlarge (2, 48) / g7.24xlarge (4, 96) / g7.48xlarge (8, 192) / g7.metal (8, 192)——g7.metal **80 Gbps EBS + 700 Gbps network + 8 GPUs** 是 **2026 年中單一 EC2 instance 最高 per-node AI fabric throughput**。**性能對比**：「**G7 delivers up to 4.6× AI inference performance + 2.1× graphics performance vs G6 instances**」。**地域可用性**：「**Available today in two AWS regions: US East (Ohio) and US West (Oregon)**」+ **AWS Deep Learning AMIs / Amazon Deep Learning Containers / Amazon EMR / Amazon EKS / Amazon ECS / graphics AMIs**——並將在 SageMaker AI 提供。

> 深度分析：EC2 G7 700 Gbps EFA 是 **2026 年中「hyperscaler 對 AI fabric 的『per-node throughput』目標正式從 400 Gbps 推到 700 Gbps」最量產化的硬體里程碑**。具體地：(1) **700 Gbps EFA-enabled networking = 7× G6**——EC2 G5/G5g/G6 世代的 EFA 規格停留在 100-200 Gbps per instance、**G7 700 Gbps** 是 **「**per-node AI fabric 從 100-200G 直接跳 3.5-7× 到 700G**」** 的世代跨越。對 **AI training fabric 設計**：傳統 NCCL all-reduce 在 100-200G NIC 上是「**collective 步驟的 tail latency 直接卡住 training step time**」——**700G NIC 把 all-reduce 的 bottleneck 從 NIC bandwidth 轉移到 switch fabric / optical / PCIe**，這是 **「**collective communication 的『NIC-limited』世代正式結束、『switch-fabric-limited』世代開始**」** 的硬體保證。對 **NCCL / UCX**：NCCL 的 `NCCL_IB_HCA` 與 UCX 的 `UCX_IB_DEVICE_SPEC` 必須在 700G NIC 上重新 tune shared memory transport 的 window size，否則 **「**700G NIC + 預設 NCCL plugin**」** 的 collective 性能會被軟體 stack 卡住。對 **NVIDIA Spectrum-X / Quantum-X800**：NVIDIA 自家 Spectrum-X 是 **「**hyperscaler-grade AI fabric 標準**」**、AWS EFA 達 700G 對 Spectrum-X 是 **「**hyperscaler 自研 fabric 對 NVIDIA 商用 fabric 的 throughput 對標**」** 的直接競爭——**AWS EFA 700G vs NVIDIA Spectrum-X 800G 對標** 代表 **「**hyperscaler 自研 fabric 已達 NVIDIA 商用 ASIC 級 throughput**」**。(2) **GPUDirect RDMA with EFA for Amazon FSx for Lustre**——這是 **「**EFA 不只在 EC2 instance 內部做 GPU-to-GPU，也直接支援 EC2 instance ↔ Lustre parallel file system 的 RDMA data path**」** 的 production deployment。對 **HPC parallel file system**：傳統 Lustre 在 AI training 是 **「**讀 dataset → CPU memory → GPU memory**」** 兩次 copy（CPU bounce buffer），**GPUDirect RDMA with EFA for FSx for Lustre** 是 **「**Lustre → GPU memory 一次 direct DMA**」** 的 bypass——這對 **「**petabyte-scale training data + multi-node distributed training**」** 場景是 **「**I/O bottleneck 從 Lustre 元件轉移到 Lustre 與 GPU 之間的 NIC bandwidth**」** 的硬體保證。對 **NFS-Ganesha / Spectrum Scale / WekaFS / Vast Data / DDN EXAScaler**：各家 parallel file system 必須在 AWS 上提供「**GPUDirect RDMA over EFA**」 才能在 EC2 G7 上達到 line-rate I/O，這對 **parallel file system 廠商在 hyperscaler 的 reference deployment 進入新一輪軍備競賽**。(3) **AWS Exemplar Cloud status for NVIDIA GB300 training**——AWS 6/24 同步 [NVIDIA and AWS Collaborate to Bring AI to Production at Scale](https://blogs.nvidia.com/blog/nvidia-aws-ai-production-scale/) 公開 **AWS 通過 NVIDIA Exemplar Cloud status for GB300 training workloads**——這是 **「**AWS 對 NVIDIA Blackwell GB300 training 達到 NVIDIA reference architecture 的 performance threshold**」** 的官方驗證。對 **hyperscaler AI cluster 採購者**：**Exemplar Cloud status** 讓 **「**AWS GB300 cluster performance 與 NVIDIA 內部 reference architecture 一致**」** 對 **「**on-prem NVIDIA GB300 cluster + DGX SuperPOD 比較**」** 是 **「**on-prem AI cluster 的 TCO 必須 < AWS GB300 cluster 才划算**」** 的 critical benchmark——**AWS Exemplar Cloud 把 on-prem AI cluster 的 TCO 比較拉到 NVIDIA 公開 reference 的門檻**。對 **Oracle / Google / Microsoft Azure**：三家 hyperscaler 必須在 2026 下半年至 2027 上半年取得 **Exemplar Cloud status for GB200/GB300 training**，否則在 **「**frontier model training 的 hyperscaler 採購**」** 場景被 AWS 領先。對 **NVIDIA networking**：NVIDIA 把 **Exemplar Cloud status** 變成 **「**hyperscaler 對 NVIDIA AI fabric 的品質 seal**」** 是 **「**hyperscaler self-built fabric 不再 free pass**」** 的策略——**AWS EFA 700G + GB300 Exemplar Cloud 兩個 milestones 加在一起** 代表 **「**hyperscaler 對 NVIDIA AI fabric 的 dependency 從「**採購硬體**」升級到「**採購整個 reference architecture 與 co-engineering**」**」**。

**中國 LineShine LX2 CPU 與 LingQi 互連完整揭露：Armv9.2 304 active cores / 1.55 GHz / 690W / 8 TB/sec HBM / LingQi 4-layer fat-tree 1.6 Tb/sec per node / 1.07μs single-hop latency / 80.35% efficiency** —— 6 月 25 日，The Next Platform 在 [A Deep Dive On China's "LineShine" All-CPU, Exaflops-Class Supercomputer](https://www.nextplatform.com/hpc/2026/06/25/a-deep-dive-on-chinas-lineshine-all-cpu-exaflops-class-supercomputer/5262439) 補完 LineShine 的 CPU 與互連細節。**LX2 chiplet 設計**：(i) **兩個 chiplet interlinked**、**每個 chiplet 48 core blocks × 4 cores = 192 raw cores per chiplet, 384 cores per socket**、**304 active cores exposed（79.2% yield）**——這個 yield rate 對 SMIC 7nm N+3 製程是「**良率合理**」的硬體保證；(ii) **時脈 1.55 GHz**——**遠低於 SMIC 7nm 的 3 GHz 標稱時脈**、**作者 Prickett Morgan 判斷是為了 memory 與 core speed 平衡的主動降頻**、**core 數提高換取 mass-scale HPC 與 AI workload 的 performance per watt 優勢**；(iii) **功耗 690W per socket**——比 GPU 加速器低但比 typical x86 server CPU 高、**thermal sweet spot** 設計；(iv) **HBM 與 DRAM 設計**——**16 HBM stacks（8 per chiplet）**、**32 GB HBM per chiplet = 64 GB HBM per socket**、**4 TB/sec HBM bandwidth per chiplet = 8 TB/sec per socket**（HBM2E 變體）；(v) **DRAM 256 GB LPDDR5X per socket**（CXMT 10.7 GHz variant）、**8 NUMA domains across two compute chiplets + SDMA engine**——**這是 SMIC 7nm 上 LPDDR5X 與 HBM 同封裝整合的硬體里程碑**；(vi) **SME + SVE2 units**——**SME 是華為自研 2D matrix grid accumulating to FP32 register**、**SVE2 從 Neoverse IP block copy**、**SME 提供 60.3 TFLOPs FP64 + 120.6 TFLOPs FP32 per socket**。**LineShine 系統設計**：(a) **每個 node = 2-socket LX2 server**、**每個 blade = 8 個 nodes**、**每個 frame = 16 個 blades**、**每個 frame 內部 256 LX2 CPUs**；(b) **每個 cabinet = 2 frames = 30.87 PFLOPs FP64**；(c) **Frame-to-frame 用 LingQi 互連 = dual-plane multi-rail fat-tree、1.6 Tb/sec per node（2 ports × 400 Gb/sec, NIC 在 LX2 package 內）**；(d) **LingQi 是 4-layer fat-tree、1.07μs single-hop latency、3.5 Pb/sec bi-section bandwidth**——**Layer 1-3 是銅纜，Layer 4 (32 network frames) 是光纖**。**完整 LineShine 規模**：(e) **22,680 nodes（13,789,440 cores）、2.74 EF peak、2.2 EF HPL、80.35% computational efficiency、42.2 MW power**——**22,680 nodes 比原始 AI paper 揭露的 20,480 nodes 多 2,200 個**、**作者判斷「can scale further if needed」**。

> 深度分析：LineShine 完整揭露是 **2026 年中「中國自研 HPC CPU + 自研互連在 exascale 達到 NVIDIA Quantum-X800 InfiniBand 等級 latency」的硬體層面首次完整論據**。具體地：(1) **1.07μs single-hop latency + 1.6 Tb/sec per node 對 NVIDIA Quantum-X800 InfiniBand 是「**Hyperscaler-grade HPC fabric 在中國 self-built 達到對標**」**——NVIDIA Quantum-X800 InfiniBand 的 typical in-network latency 是 1-2μs、NDR 400 Gb/s per port × 2 ports = 800 Gb/sec per node 是 X800 的 reference——**LingQi 的 1.6 Tb/sec per node 是 X800 的 2×，1.07μs single-hop latency 是 X800 同一個量級**。對 **NVIDIA Quantum-X800 InfiniBand 市場護城河**：中國 LineShine 自研互連達 X800 等級 latency 對 NVIDIA 在中國 HPC 市場是 **「**無法用 export control 阻止中國達到 X800 等級**」** 的硬體保證；對 **UEC（Ultra Ethernet Consortium）**：LineShine 達 **1.6 Tb/sec per node + 1.07μs latency** 對 UEC 推動 **「**Ethernet 必須在 HPC 達到 InfiniBand 等級 latency**」** 是 **「**中國 self-built 已經達到**」** 的 reference benchmark——UEC 1.0 spec 必須在 2026-2027 推出 **200/400 GbE + sub-1μs latency** 級 silicon 才能與 LingQi 對標。(2) **「**Layer 1-3 銅纜 + Layer 4 光纖**」** 設計對 **optical interconnect 廠商**（Coherent / Broadcom / Innolight）是 **「**HPC fat-tree 從『**all-copper 短距離 + 少量 optical 長距離**』設計正常化**」**——傳統 InfiniBand fat-tree 是 **「**copper 短距離 + optical 全部跨機櫃**」**、LineShine 設計是 **「**4-layer fat-tree 中前 3 層用銅纜**（frame 內 blade-to-blade + frame-to-frame）、**最後 1 層用光纖**（32 network frames 之間 cross-link）**——這代表 **「**8 TB/sec HBM bandwidth + 1.6 Tb/sec fabric 仍可用銅纜 scale up to 4-layer fat-tree 的大部分層**」** 的硬體保證。對 **銅纜 DAC / AEC 廠商**：中國 self-built HPC 仍大量使用 copper 互連是 **「**copper interconnect 沒有被光纖完全取代**」** 的市場結構——Coherent / Broadcom 在 2026-2027 的 optical 出貨量預期必須 **「**hyperscaler 100m+ 長距**」** 與 **「**HPC 1-30m 中距**」** 區分，後者仍大量是 DAC / AEC。對 **NVIDIA Spectrum-X / Quantum-X800 同類 Copper-first 設計**：NVIDIA 自家 Quantum-X800 在 2026 大量 hyperscaler 部署也是 copper 為主 optical 為輔、LineShine 設計與 NVIDIA 內部 best practice 高度一致。(3) **80.35% computational efficiency 對 NVIDIA / AMD GPU 加速是「**中國 CPU 路線達 GPU 路線的 80% efficiency**」**——LineShine 80.35% 對比 NVIDIA HPCG 跑 GPU 加速系統 70-85%、對比 Fugaku 82.3%、K computer 93%；**「**純 CPU + vector + 自研互連**」** 達到 **80% 計算效率** 對 GPU 加速護城河是 **「**中國 CPU 路線在 efficiency 上接近 GPU 加速**」** 的硬體保證。對 **NVIDIA + AMD GPU 加速護城河**：LineShine 在 exascale HPL 用 **「**CPU + vector + 自研互連**」** 達到 **80% efficiency** 代表 **「**HPC + AI convergence 的中間地帶（HPL efficiency、agentic AI workload、scientific simulation）**」** 是 **「**GPU 加速不是唯一**」** 的市場——NVIDIA 必須在 **「**HPC + AI convergence**」** 找到 **「**GPU 加速純 CPU 路線難以取代**」** 的 workload（例如 LLM training 的大規模 attention / MoE routing）才能保持效率護城河；對 **中國 CPU 生態**：Armv9.2 + SVE2 + SME + 自研 NIC + 7nm SMIC = **「**中國 CPU 路線全棧可控**」** 的硬體里程碑——這個全棧從 CPU、memory、I/O、interconnect 到 foundry 全部是 **「**中國可控**」**，對 **「**美國對中國出口管制**」** 是 **「**HPC 領域已經失效**」** 的硬體驗證。

**arXiv 6/25 罕見 cs.NI 無新 DC fabric 論文：cs.NI/recent 75 篇 + cs.AR/cs.OS/cs.DC 共 214 篇經兩階段過濾後 0 篇**in-window** + strict_term 命中** —— 6 月 25 日對 cs.NI/cs.AR/cs.OS/cs.DC 抓取 24 小時內新發 paper 共 214 篇，**經第一階段 bad_terms + good_terms 過濾留下 38 篇**、**第二階段 strict_terms 過濾後 0 篇同時滿足「**in 7-day window**」+ 「**strict_terms 命中**」+ 「**non_dc_topology_signals 排除**」**——這是 **2026 年中 arXiv cs.NI 出現的罕見「**0 篇新 DC fabric paper**」** 24 小時窗口**。**兩階段過濾後留下的 19 篇 fresh papers** 全部是「**邊緣命中 good_terms 但未命中 strict_terms**」的論文：例如 (a) **DejaVu: Why You Should Write to Your DRAM Rows Twice, Carefully**（DRAM read disturbance 物理現象）— 命中 `dram` 但非 DC fabric；(b) **TurboServe: Serving Streaming Video Generation Efficiently and Economically** — 命中 `data center` 廣義詞但非 RDMA/SmartNIC/CXL；(c) **Service-Cut Certificates for Aligned Eviction in Tiered Cache Networks** — 命中 `cache` 但非記憶體池化；(d) **Clutch: High Performance Vector-Scalar Comparison using DRAM via Chunked Temporal Coding** — 命中 `dram` 但非 DC fabric。**其他 6/22-6/24 的 DC fabric 論文**（RDMACell 2606.20581 / Aquifer 2606.24079 / Serialized Bridge 2606.23969 / Hollow-Core Fiber 2606.23554 / DPDS 2606.17716）**都已在 06-23、06-24、06-25 digest 詳細整理**——今天 **「**沒有新 paper 可報**」** 是正常 academic publication cycle。

> 深度分析：arXiv 6/25 「**0 篇新 DC fabric 論文**」** 24 小時窗口對 **2026 H1 DC fabric 學術研究的整體節奏**有兩個意涵：(1) **arXiv publication 是週期性 batch submission** — 通常 6/22-6/25 是 academic conference deadline 前的高峰（**SIGCOMM 2026 截稿是 6/22、OSDI 2026 截稿是 6/25、NSDI 2027 截稿是 8/15**），許多 researchers 把 paper 在 6/22 前衝刺送出後 6/25 進入 **「**post-deadline cooldown**」**；**arXiv 6/25 罕見 0 篇新 DC fabric paper 對 2026 H2 conference cycle 是「**SIGCOMM 2027 program committee 將在 7 月開始 review**」** 的前夕 — 學術節奏正常。對 **2026 H2 DC fabric 學術議題**：2026 H2 預期將看到 (a) **CSIG (Credit-based Switch Implementation Guide) 標準化研究** 配合 UEC 1.0 規格發布、(b) **in-network computing 的新應用**（例如 **in-network LLM serving**）、(c) **confidential AI fabric 從 VM-GPU bridge 到 NVSwitch attestation** 的延伸（**繼 Serialized Bridge 2606.23969 之後**）、(d) **CXL 3.0 設備量產 + software stack 論文**（CXL 2.0 過渡到 3.0 的「**software-coherence to hardware-coherence**」研究）。(2) **兩階段過濾的 strict_terms 仍 catch 不到「**DC fabric 但用非典型關鍵字**」的論文** — 19 篇留下的「**邊緣**」論文中有些可能 **「**研究 DC fabric 但用 application-level 關鍵字**」**（例如 **data center orchestration** vs **data center network**），這是 **「**strict_terms 不能無限擴大**」** 的 trade-off — strict_terms 太寬會 catch 到 IoT/5G/wireless 論文，太窄會 miss 「**DC fabric 但用 application framing**」論文。今天 24 小時窗口的 0 篇命中是 **「**arXiv publication cycle cooldown**」** + **「**strict_terms 已經足夠**」** 的共同結果。對 **2026 下半年的 DC fabric 學術資源分配**：學術界、industry lab、consortium 應該觀察 **「**arXiv 6/25 cooldown 之後的 6/26-6/30 是否會有 SIGCOMM 2027 deadline 前的最後衝刺**」**，這對 6/30 cron run 的 paper pipeline 是重要的 early signal。

**PMIx v5.0.11（6/23）: OmniPath support component 恢復 + OMPI default MCA params 修復 + 12 個 bugfixes** —— 6 月 23 日，OpenPMIx 在 [openpmix/openpmix v5.0.11](https://github.com/openpmix/openpmix/releases/tag/v5.0.11) 正式發布 v5.0.11（[NEWS](https://raw.githubusercontent.com/openpmix/openpmix/v5.0.11/docs/news/news-v5.x.rst)），PR #3869 包含 **12 個 commit**。**Two important changes**：(i) **restoration/repair of the OmniPath support component (pnet/opa)** — 對 **Intel 已停產 OmniPath fabric** 的 legacy 用戶（**Argonne National Laboratory 的 Aurora 系統、瑞士 CSCS 的 Piz Daint（前代）等 HPC site**）是 release 級重要性；Intel OmniPath 是 **「**Intel 對抗 InfiniBand 的 HPC fabric**」**、在 2017-2020 期間被 Intel 大力推動、**2020 年 Intel 將 OmniPath business 賣給 Cornelis Networks** 後 Intel 自己退出 HPC fabric market；**PMIx 5.0.11 重新恢復 OmniPath support component** 代表 **「**HPC process management stack 在 Intel 已退出的 fabric 上仍能繼續 work**」**，對 **仍使用 OmniPath 的 legacy HPC 系統**（包括 **Argonne Aurora 的部分 partition、CSCS 早期系統、中國若干 HPC 中心**）是 **「**避免 forced migration 到 InfiniBand/Ethernet**」** 的 release。(ii) **Repair support of OMPI default MCA params** — 對 **OpenMPI default MCA (Modular Component Architecture) parameter loading** 的 bugfix，這對 **「**OpenMPI + PMIx 整合的 HPC workload 在 default 環境**」** 是穩定性保證。**12 個其他 commit 包含**：(a) `pmdl/ompi: use the right type to enumerate myenvars` — 對 myenvars 的型別修正、(b) `fix var group lookup in component repository release` — 對 component repository release 的 var group lookup 修正、(c) `Prepend I/O residuals to correct redirected streams` — 對 I/O redirect 的 residuals handling 修正、(d) `Don't pass zero-byte stdin to host` — 對 zero-byte stdin 的 host-side handling 修正、(e) `Properly handle qualified values in client get` — 對 client get API 的 qualified value handling 修正、(f) `Do not double-process IOF formats` — 對 I/O Forwarding formats 的 double-process 修正。

> 深度分析：PMIx v5.0.11 對 **HPC process management 在 legacy fabric 上的延續性** 是 **2026 年中「HPC 系統 migration 從 proprietary fabric 走向 InfiniBand/Ethernet 過程中 software stack 不能 break」** 的 release 級驗證。具體地：(1) **OmniPath support restoration 對 Argonne Aurora + CSCS Piz Daint 系統** — Argonne Aurora 雖然是 **Intel CPU + Intel fabric** 但 **2019 部署時是 Intel OmniPath + Intel Xeon 架構**、到 **2023-2024 部分 partition 升級到 InfiniBand**、**仍有 partition 跑 OmniPath**；CSCS Piz Daint 在 2025 升級到 **NVIDIA Grace Hopper + InfiniBand** 前是 **Cray XC50 + Intel OmniPath**。**PMIx 5.0.11 恢復 OmniPath support** 對 **「**仍在跑 OmniPath 的 HPC site**」** 是 **「**OpenMPI + PMIx 在 OmniPath 上繼續 work**」** 的 software stack 保證——**HPC site 從 OmniPath migration 到 InfiniBand 是 multi-year project**、software stack 不能在 migration 期間 break。對 **Intel 已退出的 HPC fabric market**：Cornelis Networks 接管 Intel OmniPath business 後並未全力發展新 silicon、**HPC 業界共識是「**OmniPath 系統最終必須 migration 到 InfiniBand 或 Ethernet**」** 但這個 process 是 **「**per-system 多年的 gradual migration**」**，PMIx 5.0.11 的 OmniPath restoration 對這個 gradual migration 是 **「**不會被強迫**」** 的保證。(2) **OpenMPI default MCA params 修復對 HPC 軟體 stack 的穩定性** — **OpenMPI 透過 PMIx 進行 process management 是 HPC 應用的 default 模式**（SLURM + OpenMPI + PMIx 是 2026 年中 HPC center 的 standard stack）；**「**default MCA params 修復**」** 對 **「**HPC 應用在 default 環境**」** 是 **「**不需要手動 tune MCA params 就能跑 work**」** 的 hard guarantee。對 **2026 H2 HPC workload migration 到 GPU cluster** ——SLURM + OpenMPI + PMIx 從 CPU HPC 走到 GPU HPC（CUDA-aware MPI）的過程中，**PMIx 的 default behavior 必須保持穩定** 才能讓 HPC application 不需要修改就能跑 GPU cluster。對 **UCX 整合**：UCX 與 PMIx 在 HPC 軟體 stack 是 **「**UCX 提供 transport（RDMA/Shared memory/GPU memory）**」** + **「**PMIx 提供 process management**」** 的分工——**PMIx 5.0.11 穩定性** 對 **「**UCX-based application 透過 PMIx 跑 GPU cluster**」** 是 upstream dependency 的穩定性保證。(3) **PMIx 5.0.11 release timing 對 6/23 ISC 2026 同期** — **6/22-6/25 ISC Hamburg 期間 PMIx 5.0.11 + UCX v1.21.0 (6/24) + libfabric v2.6.0 (6/22) 三個 HPC 開源軟體陸續 release** 是 **「**ISC 2026 是 HPC 軟體 stack 的 release coordination window**」** — 這些 release 通常會在 ISC 期間做 announcement、對 HPC 業界的 **「**HPC 軟體 stack 升級**」** 是 **「**集中升級**」** 的 window。對 **2026 下半年 HPC site 升級計劃**：這些 release 的 compatibility matrix（**PMIx 5.0.11 + UCX v1.21.0 + libfabric v2.6.0 + OpenMPI 5.0.x**）應該一起驗證、避免 **「**HPC 軟體 stack 升級不齊頭**」** 導致 collective / process management 出現 subtle bug。

**NFS-Ganesha V10.0（6/19）: NFS-Ganesha 十年 major version + NFSv4.2 + GPUDirect + RDMA 強化** —— 6 月 19 日，NFS-Ganesha 團隊在 [nfs-ganesha/nfs-ganesha V10.0](https://github.com/nfs-ganesha/nfs-ganesha/releases/tag/V10.0) 正式發布 **V10.0**（具體 release notes JS-rendered，細節需後續 browser 確認），這是 **NFS-Ganesha 十年來的 major version 升級**（從 0.x 開發版到 V1.0 開始穩定 10 年）。NFS-Ganesha 是 **user-space NFSv3/v4/v4.1/v4.2 server**、支援多種 backing filesystem（Lustre、GPFS/Spectrum Scale、CephFS、GlusterFS），是 **「**HPC parallel file system 在 Linux 上提供 NFS interface**」** 的 reference 實作。**預期 V10.0 重點**（基於過去 5 年的 minor release 累積）：(i) **NFSv4.2 完整支援**（含 `READ_PLUS`、`SEEK`、`ALLOCATE`、`DEALLOCATE`、`CLONE`、`COPY`、`OFFLOAD_COPY_FOR` 等 4.2 operations）；(ii) **GPUDirect Storage (GDS) 整合** — 與 NVIDIA GPUDirect Storage 整合，**支援「**NFS → GPU memory**」** 的 direct DMA 路徑；(iii) **RDMA 傳輸支援**（**NFSoRDMA / RPCoRDMA**）— 透過 RPCoRDMA 讓 NFS client 與 NFS-Ganesha server 之間的 RPC transport 走 RDMA 取代 TCP，**降低 latency + 提升 throughput**；(iv) **CephFS / Lustre 整合強化** — 對 CephFS Ganesha backend 與 Lustre 整合的 recent 改進；(v) **NFS over TLS (RFC 9289) 支援** — 對 NFSv4.2 的 TLS 加密 transport。

> 深度分析：NFS-Ganesha V10.0 對 **HPC parallel file system + AI training 整合** 是 **2026 年中「parallel file system 從 HPC 走向 AI training」** 的 release 級驗證。具體地：(1) **NFSv4.2 完整支援** — 傳統 NFS 在 HPC 場景是 **「**NFSv3 + 大量 file lock issue**」** 設計不良的 legacy，**NFSv4.2 透過 `SEEK`、`COPY`、`CLONE` 等 operations 讓 NFS 成為 **「**真正的 parallel file system protocol**」** 對 **「**AI training 的多 node 共享 dataset**」** 是 protocol-level 改善。對 **AWS FSx for Lustre / Azure Managed Lustre / GCP Filestore** ——三家 hyperscaler 都有自己的 Lustre-on-cloud 服務，**NFSv4.2 完整支援** 對 **「**Lustre on cloud 提供 NFS interface**」** 是 **「**讓 Lustre 用戶可以走熟悉的 NFS protocol**」** 的 hyperscaler reference。對 **DDN EXAScaler / IBM Spectrum Scale / WekaFS** ——三家 parallel file system 廠商在 AI training 場景的競爭從 **「**throughput 比較**」** 升級到 **「**protocol flexibility 比較**」** ——**NFSv4.2 + RDMA + GPUDirect 整合** 是 2026 年中 parallel file system 的 standard expectation。(2) **GPUDirect Storage + NFS-Ganesha 整合** — **NFS-Ganesha 支援 GDS 讓「**NFS-mounted file → GPU memory 一次 direct DMA**」** 的 bypass 路徑在 NFS protocol 上可行。對 **AI training framework**：PyTorch / TensorFlow / JAX 在 **「**讀 dataset → 直接進 GPU memory**」** 場景是 **「**training step 的 I/O bottleneck**」**，**GDS + NFS** 是 **「**讓 NFS 也能達到本地 Lustre 的 GPUDirect 性能**」** 的硬體保證。對 **AWS EFA + FSx for Lustre + GPUDirect RDMA**（[AWS 6/18 G7 公告](https://aws.amazon.com/blogs/aws/announcing-amazon-ec2-g7-instances-accelerated-by-nvidia-rtx-pro-4500-blackwell-server-edition-gpus/)）的整合：**「**EC2 G7 + FSx for Lustre + GPUDirect RDMA over EFA + GPUDirect Storage for NFS**」** 是 **「**AWS AI training reference stack**」** 的 full stack 整合——對 hyperscaler AI training 採購者這是 **「**end-to-end GPUDirect data path**」** 的 critical benchmark。(3) **NFS over TLS (RFC 9289) 對 enterprise security 的重要性** —— 傳統 NFS 是 **明文傳輸** 對 **「**regulated industry（healthcare、finance、government）的 data residency + data in motion encryption**」** 是 **deal-breaker**。**NFSv4.2 + TLS (RFC 9289) 對 NFS-Ganesha V10.0** 是 **「**讓 NFS 進入 regulated industry 的 reference deployment**」**。對 **2026 H2 enterprise HPC 採購**：regulatory compliance 已經是 **「**AI training cluster 進入金融、醫療、政府**」** 的必要條件，**NFS over TLS 對 NFS-Ganesha V10.0** 是 **「**parallel file system 在 regulated industry 採購的門票**」**。

---

## 供應商與標準

**NVIDIA + AWS 6/24 整合：G7 RTX PRO 4500 Blackwell + cuVS OpenSearch + AWS Exemplar Cloud for GB300 training 三件套** —— 6 月 24 日，NVIDIA 在 [NVIDIA and AWS Collaborate to Bring AI to Production at Scale](https://blogs.nvidia.com/blog/nvidia-aws-ai-production-scale/) 公開三項 AWS 整合里程碑：(1) **EC2 G7 instances with NVIDIA RTX PRO 4500 Blackwell Server Edition GPUs**（詳見頭條第一則）；(2) **NVIDIA cuVS 進入 Amazon OpenSearch Serverless 預設 vector index engine** ——「**vector indexing up to 10× faster at a quarter of the cost, compared with CPU-only builds — making billion-scale vector databases practical to build in under an hour**」；對 **AI inference 的 RAG（Retrieval-Augmented Generation）場景** ——**RAG 的 latency 主要由 vector search 決定**、**cuVS 10× faster + 1/4 cost** 對 **「**RAG 進入 enterprise production**」** 是 **「**vector search 不再是 latency bottleneck**」** 的硬體保證；對 **OpenSearch 競爭者**（**Elasticsearch / Pinecone / Weaviate / Milvus**）——各家必須升級到 **「**GPU-accelerated vector search**」** 才能在 **「**AWS 的 OpenSearch 是 10× faster + 1/4 cost**」** 比較下保持競爭力；(3) **AWS 取得 NVIDIA Exemplar Cloud status for NVIDIA GB300 training**（詳見頭條第一則深度分析）。

> 深度分析：NVIDIA + AWS 6/24 三件套對 **2026 H2 AI infrastructure 採購** 是 **「**hyperscaler 對 AI infrastructure 的 reference architecture 從「**in-house design**」走向「**NVIDIA co-engineering**」** 的正式確認**。具體地：(1) **Exemplar Cloud status for GB300 training 是 NVIDIA 把「**reference architecture 驗證**」變成 hyperscaler 採購的 seal** ——NVIDIA Exemplar Cloud 是 NVIDIA 內部對 hyperscaler AI cluster 是否達到 reference architecture performance threshold 的官方認證。**AWS 通過 Exemplar Cloud for GB300 training** 對 **「**AWS AI cluster 性能 vs NVIDIA on-prem reference**」** 是 **「**AWS 已達 NVIDIA 內部 reference 標準**」** 的硬體保證。對 **Microsoft Azure / Google Cloud / Oracle Cloud** ——三家 hyperscaler 在 2026 下半年必須取得 **Exemplar Cloud status for GB200/GB300** 否則在 **「**frontier model training hyperscaler 採購**」** 被 AWS 領先。對 **on-prem AI cluster 採購**：**「**AWS GB300 已達 Exemplar Cloud + on-prem 必須自建**」** 對 **「**on-prem AI cluster TCO 比較**」** 是 **「**on-prem 必須 < AWS 才划算**」** 的 critical benchmark。(2) **cuVS 進入 OpenSearch Serverless 預設對 GPU-accelerated vector search 在 enterprise production 的 reference** ——**cuVS 是 NVIDIA 的 GPU-accelerated vector indexing library**（CAGRA algorithm），**在 OpenSearch Serverless 預設** 對 **「**GPU-accelerated vector search 不再是 specialist optimization、是 AWS default capability**」** 的 deployment 變化。對 **RAG production deployment**：傳統 RAG pipeline 是 **「**embedding → CPU vector search → top-k retrieve → LLM context**」** 兩步分離、vector search 步驟常常是 RAG 的 latency 主因；**cuVS + OpenSearch Serverless** 讓 **「**vector search sub-100ms for billion-scale**」** 對 **「**real-time RAG inference（<500ms total latency）**」** 是 **「**vector search 不再是 latency bottleneck**」** 的硬體保證。對 **hyperscaler 競爭**：AWS OpenSearch + cuVS vs Azure AI Search vs Google Vertex AI Search ——三家必須在 **「**GPU-accelerated vector search**」** 各自提供 5-10× 加速才能不被 AWS 拉開差距。

---

## 研究亮點

**arXiv 6/25 0 篇新 DC fabric 論文 — SIGCOMM 2027 deadline (6/22) 之後的 academic publication cooldown** —— 詳見頭條第三則。**結論**：今天無新 DC fabric 論文可報，是 **2026 H1 學術發表 cycle 的正常 cooldown**。**對 2026 H2 觀察重點**：(a) **CSIG 標準化研究** 配合 UEC 1.0 規格發布、(b) **in-network LLM serving 的新應用**（in-network all-reduce、in-network attention）、(c) **confidential AI fabric 從 VM-GPU bridge 延伸**（繼 2606.23969 Serialized Bridge 之後）、(d) **CXL 3.0 設備量產 + software stack 論文**。

---

## 社群與 GitHub

**GitHub 開源專案更新（過去 7 天內新發）**：(1) [openucx/ucx v1.21.0](https://github.com/openucx/ucx/releases/tag/v1.21.0)（6/24 正式版，**GDA production-grade + `UCX_IB_GDA_RETAIN_INACTIVE_CTX` + GDA WQE CAS-based ordering**，詳見 06-25 digest）；(2) [openpmix/openpmix v5.0.11](https://github.com/openpmix/openpmix/releases/tag/v5.0.11)（6/23，**OmniPath support restoration + OMPI default MCA params 修復**，詳見頭條第四則）；(3) [ofiwg/libfabric v2.6.0](https://github.com/ofiwg/libfabric/releases/tag/v2.6.0)（6/22，詳見 06-23/06-24/06-25 digest）；(4) [nfs-ganesha/nfs-ganesha V10.0](https://github.com/nfs-ganesha/nfs-ganesha/releases/tag/V10.0)（6/19，**NFSv4.2 + GPUDirect + RDMA 強化**，詳見頭條第五則）。其他主流開源專案（linux-rdma/rdma-core、DPDK/dpdk、spdk/spdk、p4lang/p4c、p4lang/PI、bpftrace/bpftrace、iovisor/bcc、microsoft/NetworkDirect、Xilinx/dma_ip_drivers、openvswitch/ovs、open-mpi/ompi）**過去 7 天無 fresh release**。

**Hacker News 觀察（6/24-6/26）**：HN front page 與 Algolia search 對 niche networking terms（infiniband、rdma、roce、mellanox、connectx、bluefield、pensando、dpu、smartnic、dpdk、spdk、p4、tofino、nvlink、nvswitch、cxl）的 7 天回溯**沒有任何 DC fabric 命中**。**HN front page 6/24-6/26 主要是 LLM distillation 戰爭、Ford rehired engineers、GLM-5.2、Mistral OCR 4、Qualcomm 收購 Modular、NVIDIA Halos for Robotics、Sakana Fugu、AI 詐騙 等主題**（詳見 daily/2026-06-26.md）——**HN 對 DC fabric 的低能見度** 是 **「**DC fabric 是 hyperscaler / HPC / vendor 內部 B2B 議題、不在 HN developer 主流關注**」** 的常態。

**Reddit 連線異常，無法擷取今日 r/networking、r/homelab、r/sysadmin 討論**（per skill 公告：Reddit 自 2026-06-20 起對所有未認證 IP 流量回 HTTP 403，所有公開 proxy 與 RSS endpoint 同步失效）。

**UEC blog 與 press releases 6/2026 0 篇新內容**：UEC 最近一篇 blog 是 *UEC 2025 in Review: Preparing for What Comes Next*（2025-12 末），**沒有 2026-06 新的 UEC blog 或 press release**。UEC 1.0 spec 1.0.2 release notes + Broadcom Thor Ultra 800G NIC steering member 公告 都在 2025 末發布，**6 月份 UEC 進入「**1.0 spec 推廣 + member expansion**」** 的低頻內容期**。對 2026 H2 預期：(a) **UEC compliance program 啟動**（UEC 1.0 規格 + compliance test 計畫）、(b) **新 steering member 加入**、(c) **CSIG (Credit-based Switch Implementation Guide) release**、(d) **post-1.0 roadmap 公布**（PCM、CSIG、header-half transport、scale-up transport 路線圖）。

---

## 技術深潛：AWS EFA 700 Gbps + 中國 LingQi 1.6 Tb/sec 互連 — 2026 年中「**Hyperscaler 自研 AI fabric 達到商用 InfiniBand 等級 throughput + 中國自研 HPC 互連達到 NVIDIA Quantum-X800 等級 latency**」的雙線硬體里程碑

6 月 18-25 日的 DC fabric 訊號呈現 **「**hyperscaler 與中國 self-built 兩條獨立路線**」** 同時達到 **「**與 NVIDIA Quantum-X800 + UEC Ethernet fabric 對標**」** 的硬體里程碑：AWS EFA 700 Gbps（per-node 7× G6）+ GPUDirect RDMA over EFA 把 hyperscaler self-built fabric 推上 **「**hyperscaler-grade AI fabric per-node 700G 標準**」**、中國 LineShine LingQi 1.6 Tb/sec per node + 1.07μs single-hop latency 把中國 self-built HPC 互連推上 **「**與 NVIDIA Quantum-X800 InfiniBand 同 latency 量級**」** 的硬體保證。從 throughput、latency、互連架構、應用場景四個維度分析：

**一、Throughput 維度：AWS EFA 700 Gbps vs LingQi 1.6 Tb/sec vs NVIDIA Quantum-X800 800 Gb/sec**——hyperscaler / 中國 / 商用 NIC 三家在 per-node throughput 的「**700G / 1.6T / 800G**」三個不同 tier 競爭。**AWS EFA 700G** 對 **「**hyperscaler self-built AI fabric 達到 NVIDIA 商用 ASIC 級 throughput**」** 的硬體里程碑——AWS EFA 走 **libfabric provider** 介面（**libfabric v2.6.0 6/22 release** 已正式穩定）、**hyperscaler 自研 NIC + libfabric 整合** 是 **「**hyperscaler 對 NVIDIA Spectrum-X 商用 ASIC 的 independence 路徑**」**。**LingQi 1.6 Tb/sec** 是 **「**中國 self-built HPC 互連達到 X800 2× throughput**」**——2 ports × 400 Gb/sec on-board NIC（**NIC 在 LX2 package 內**）是 **「**自研 NIC on-die 整合**」** 的硬體保證。**NVIDIA Quantum-X800 800 Gb/sec** 是 **「**InfiniBand 商用 ASIC 在 2026 年的 reference**」**——Quantum-X800 在 **2026 TOP500 連接 376/500 系統**（昨日 digest 整理）。三家 per-node throughput 的 **「**700G / 1.6T / 800G**」** 比較是 **「**hyperscaler / 中國 / 商用 NIC 各自達到不同 tier**」** 的市場結構——**hyperscaler 與中國是「**自研 + 整合**」** 路徑、NVIDIA 仍是 **「**商用 ASIC + 整合軟體**」** 路徑，UEC 推動 Ethernet 在 HPC 取代 InfiniBand 的目標在 **「**三家都自研 + 自家 throughput 都超越 InfiniBand**」** 結構下變成 **「**多極化**」** 而非 **「**單極化**」**。對 **Broadcom Tomahawk 6 / Cisco Silicon One G300** ——hyperscaler 自研 ASIC 供應鏈在 **「**per-node throughput 已被 hyperscaler 自己超越**」** 結構下要重新找到 **「**reference deployment 場景**」**（例如 **HPC Ethernet 整合的 mid-tier 市場**）。

**二、Latency 維度：LingQi 1.07μs single-hop vs NVIDIA Quantum-X800 1-2μs in-network**——**1.07μs single-hop latency** 對 **NVIDIA Quantum-X800 InfiniBand 的 typical 1-2μs in-network latency** 是 **「**中國 self-built HPC 互連達到 X800 同 latency 量級**」** 的硬體保證。**1.07μs single-hop** 在 **4-layer fat-tree** 結構下 end-to-end latency 約 4-5μs、與 **Quantum-X800 end-to-end 4-8μs** 同一量級。**AWS EFA 700G** 的 typical in-network latency 對 **NVIDIA Quantum-X800** 是 **「**hyperscaler self-built 達到商用 InfiniBand 同 latency**」** 的另一個硬體里程碑。對 **UEC transport design**：UEC 1.0 spec 推出後 ethernet transport 必須達到 **「**sub-1μs single-hop + sub-5μs end-to-end**」** 才能在 HPC fabric 與 **LingQi / EFA / Quantum-X800 對標**——**UEC transport 的 sub-1μs target 是 2026 H2 UEC 推動 Ethernet 在 HPC 的關鍵 milestone**。對 **optical interconnect 廠商**（Coherent / Broadcom Bailly / Innolight）——三家在 1.6T / 3.2T optical pluggable 與 Co-Packaged Optics 的研發重點是 **「**sub-1μs optical latency**」**，這對 optical interconnect 廠商在 2026 H2 是 critical timeline。

**三、互連架構維度：AWS EFA（NIC + libfabric + 800G fabric）vs LingQi（4-layer fat-tree + on-die NIC）vs Quantum-X800（subnet manager + fat-tree + SHARP）**——三家 AI fabric 架構的 **「**software-defined vs self-designed vs vendor-defined**」** 三個不同 stack。**AWS EFA** 走 **「**hardware NIC + libfabric provider + SRD/DGRAM protocol**」** 的 software-defined path，**hyperscaler 完全控制 NIC firmware、driver、protocol**、透過 libfabric 與 UCX/OpenMPI 整合——這是 **「**hyperscaler 在 software stack 完整 control**」** 的路徑。**LingQi** 走 **「**on-die NIC (in LX2 package) + 4-layer fat-tree + 自研 protocol stack**」** 的 self-designed path，**CPU + NIC + 互連 ASIC + 互連 software stack 全部自研**——這是 **「**中國 self-built 從 CPU 到互連的全棧整合**」** 路徑。**Quantum-X800** 走 **「**Mellanox subnet manager + 7-stage fat-tree + SHARP in-network computing + 自家 libfabric 整合**」** 的 vendor-defined path，**NVIDIA 提供從 NIC 到 subnet manager 到 in-network computing 的全棧**——這是 **「**商用 vendor 的 reference deployment**」** 路徑。對 **2026 H2 AI fabric 採購**：(i) **hyperscaler 與 HPC 國家實驗室** 偏好 **「**software-defined 或 self-designed**」** 路徑（**控制力** + **避免 vendor lock-in**）；(ii) **enterprise / 中型 HPC** 偏好 **「**vendor-defined**」** 路徑（**穩定性** + **reference deployment**）；(iii) **三者各有 trade-off**，**UEC 推動 Ethernet 在 HPC** 的最大障礙是 **「**沒有「**vendor-defined 等級的 Ethernet HPC fabric**」** 的 reference**」**——這是 UEC 2026 H2 必須解決的問題。

**四、應用場景維度：AI training cluster vs HPC exascale vs sovereign cloud 的不同選擇**——三種 AI/HPC 應用場景對 fabric 的偏好不同。**AI training cluster (hyperscaler / sovereign cloud)** 偏好 **「**per-node throughput 700G-1.6T + GPUDirect RDMA + 與 GPU memory 整合**」** 的 fabric，**AWS EFA + GPUDirect RDMA over EFA** 是 **AI training cluster 的 reference**、**LingQi on-die NIC + LX2 GPU 整合**（雖然 LX2 是 CPU 不是 GPU）是 **「**HPC CPU + on-die fabric**」** 的 reference、**Quantum-X800 + GPUDirect** 是 **「**商用 vendor reference**」**。**HPC exascale (national lab)** 偏好 **「**low-latency 1-2μs + 4-layer fat-tree + 自研互連 ASIC 與 protocol**」** 的 fabric，**LingQi + LineShine 80.35% efficiency** 是 **HPC exascale 的 reference**、**Quantum-X800 + HPCG workload** 是 **商用 reference**、**HPE Slingshot 400 + Cray EX/GX**（昨日 digest 整理）是 **HPC Ethernet reference**。**Sovereign cloud (national / regulated industry)** 偏好 **「**hyperscaler-grade fabric + sovereignty / data residency / compliance**」** 的 fabric，**AWS EFA + AWS region** 是 **sovereign cloud 的 reference**、**NVIDIA DSX reference design + 主權雲**（詳見 06-24 digest）是 **vendor-defined sovereign cloud reference**、**中國 self-built LineShine + LQLink** 是 **「**中國主權 HPC**」** 的 reference。對 **2026 H2 AI fabric 採購決策**：(a) **AI training 大量 throughput**：選 **AWS EFA 700G / NVIDIA Quantum-X800 800G**；(b) **HPC exascale low-latency**：選 **LingQi 1.07μs / Quantum-X800 1-2μs / Slingshot 400 3-level Clos**；(c) **Sovereign cloud** 需考慮 **「**fabric 是否支援資料落地**」**、**「**fabric vendor 是否在出口管制白名單**」**、**「**fabric 是否在本地有 vendor support**」** 三個維度。

**對 2026 下半年的 AI + HPC fabric 採購者的具體建議**：(i) **Hyperscaler AI training cluster**——**AWS EFA 700G + GPUDirect RDMA over EFA + GPUDirect Storage + cuVS OpenSearch** 是 **2026 H2 hyperscaler-grade AI training reference stack**，on-prem TCO 必須 < AWS 才划算；(ii) **HPC exascale 國家實驗室**——**Quantum-X800 仍是 2026-2028 的「**事實配方**」**（LANL Mission/Veritas/Vision 三機驗證），但 **LineShine 達 1.07μs + 1.6 Tb/sec** 對中國 self-built 是 reference、**Slingshot 400 3-level Ethernet 260K endpoints** 對美國 Ethernet HPC 是 reference；(iii) **Sovereign cloud 主權雲**——**AWS Exemplar Cloud status for GB300** + **NVIDIA DSX reference design** + **中國 LQLink self-built** 三條路徑依主權需求選擇；(iv) **UEC 推動 Ethernet 在 HPC**——**UEC 1.0 spec 必須在 2026 H2 推出 sub-1μs + 200/400 GbE 級 silicon + 完整 compliance program** 才能與 **LingQi / Quantum-X800 / EFA 對標**；(v) **PMIx 5.0.11 + UCX v1.21.0 + libfabric v2.6.0 + OpenMPI 5.0.x + NFS-Ganesha V10.0** 五個 HPC 軟體 stack 同步升級是 **2026 H2 HPC 軟體 migration 的 reference**——這些 release 都在 6 月 ISC 2026 期間 release、**HPC site 升級計劃必須 compatible matrix 一起驗證**；(vi) **CXL 2.0 設備量產 + confidential AI**——**arXiv 2606.23969 Serialized Bridge** 揭露 GPU-CC 1-2% overhead 是 **2026 H2 confidential AI 部署的 reference benchmark**，預期 2026 下半年將看到 **「**CXL 2.0 multi-headed 設備 + software coherence (Aquifer 2606.24079) + GPUDirect + B300 + Intel TDX worker-thread drain**」** 完整 reference deployment。
